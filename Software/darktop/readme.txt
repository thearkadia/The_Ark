First install the dependencies:
sudo apt install libqt5*

Then you can install the deb:
sudo dpkg -i darktop_0.0.1.deb

Finally execute:
darktop

Note: the only was to exit darktop is to connect via ssh and kill the process (as the application is running in fullscreen)